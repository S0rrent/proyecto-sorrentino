node_modules
dist
# .env se versiona a propósito (solo contiene la anon key pública de Supabase).
# NUNCA agregar aquí la service_role key.
.env.local
.env.*.local

# LLM Council artifacts en raíz (decisiones importantes van a docs/council/)
council-report-*.html
council-transcript-*.md
